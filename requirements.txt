streamlit>=1.31.0
pandas>=2.1.0
PyPDF2>=3.0.1
openpyxl>=3.1.2
python-dateutil>=2.8.2
```

**What changed?**
- Changed `==` to `>=` (allows compatible newer versions)
- Changed `pandas==2.2.0` to `pandas>=2.1.0` (compatible with Python 3.13)

---

### **File 2: Create `.python-version`**

Create a NEW file in your project root called `.python-version` (with the dot at the start):
```
3.11
```

This tells Streamlit Cloud to use Python 3.11 instead of 3.13.

---

## 📂 **Your Project Structure Should Now Be:**
```
quote-pdf-extractor/
├── app.py
├── requirements.txt          ← UPDATED
├── .python-version          ← NEW FILE
├── .gitignore
├── README.md
└── .streamlit/
    └── config.toml
